using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using wickedcrush.menu.editor;
using wickedcrush.display.sprite;

namespace wickedcrush.factory.menu.editor
{
    public class NodeFactory
    {
        EditorMenu menu;

        public NodeFactory(EditorMenu menu)
        {
            this.menu = menu;
        }

        public void AddNode(String text)
        {
            //MenuNode node = new MenuNode(new TextSprite(, 
        }
    }
}
